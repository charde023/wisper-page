# Workspaces — per-video artifacts, large + per-run, not tracked
workspaces/

# Source video storage — large mp4/m4a recordings, not tracked
영상자료/

# Collected clean transcripts — per-video result md, not tracked
결과물/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Editor/OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Whisper model downloads (if anyone caches locally)
*.bin
*.pt

# Misc
*.log

# Workflow local config (user-specific overrides; config.example.json is committed)
workflow/config.json

# YouTube pipeline — machine-local state (channel_index, curated_queue, seen_videos) + local config
workflow/youtube/state/
workflow/youtube/config.json
